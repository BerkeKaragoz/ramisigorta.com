---
import Base from "../layouts/Main.astro"
import BaseHead from "../components/BaseHead.astro"
import Header from "../components/Header.astro"
import Footer from "../components/Footer.astro"
import ServiceCard from "../components/ServiceCard.astro"
import { defaultToInt } from "@/lib/numberOperations"

const DEFAULT_PAGE = 1
const DEFAULT_PAGE_SIZE = 9

const page = defaultToInt(Astro.url.searchParams.get("page"), DEFAULT_PAGE)
const pageSize = defaultToInt(
   Astro.url.searchParams.get("page_size"),
   DEFAULT_PAGE_SIZE
)
---

<Base
   class="app-container mt-20 grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3 max-w-6xl"
>
   <ServiceCard />
   <ServiceCard />
   <ServiceCard />
   <ServiceCard />
   <ServiceCard />
   <ServiceCard />
   <ServiceCard />
   <ServiceCard />
   <ServiceCard />
</Base>
